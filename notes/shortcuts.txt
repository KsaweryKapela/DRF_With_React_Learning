alf + left/right = move tab
ctr + f4 = close tab

shift + shift = search for file
f12 = terminal
escape = escape terminal

ctr + w = select chunk of text
ctr + left/right = jump word

shift + f10 = run
